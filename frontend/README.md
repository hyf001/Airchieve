# AIrchieve Frontend

React + Vite + Tailwind CSS frontend scaffold using shadcn/ui conventions.

```bash
npm install
npm run dev
```

The app uses state-based view switching instead of React Router.
